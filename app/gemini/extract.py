"""Gemini OCR provider placeholder implementation."""

from __future__ import annotations

def pdf_to_markdown(pdf_path: str) -> tuple[str, dict[str, object]]:
    """Return a clear placeholder until Gemini extraction is implemented."""
    _ = pdf_path
    raise NotImplementedError(
        "Gemini provider is not implemented yet. "
        "Start with `--providers mistral` and add Gemini next."
    )

